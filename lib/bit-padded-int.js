// Generated by CoffeeScript 1.6.3
var BitPaddedInt;

BitPaddedInt = function(value, bits) {
  var byte, bytes, i, mask, numeric_value, shift, _i, _ref;
  if (bits == null) {
    bits = 7;
  }
  mask = (1 << bits) - 1;
  bytes = [];
  for (i = _i = 0, _ref = value.length; 0 <= _ref ? _i < _ref : _i > _ref; i = 0 <= _ref ? ++_i : --_i) {
    byte = value.charAt(i);
    bytes.push(byte.charCodeAt(0) & mask);
  }
  bytes.reverse();
  numeric_value = 0;
  shift = 0;
  bytes.forEach(function(byte) {
    numeric_value += byte * Math.pow(2, shift);
    return shift += bits;
  });
  return numeric_value;
};

module.exports = BitPaddedInt;
